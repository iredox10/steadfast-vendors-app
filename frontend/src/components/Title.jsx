import React from 'react'

const Title = ({title}) => {
  return (
    <div>
        <h1 className="text-center  capitalize font-bold text-2xl md:text-4xl">{title}</h1> 
        </div>
  )
}

export default Title